services:
  larschat:
    build: .
    env_file:
      - path: .env
        required: false
    ports:
      - "3000:3000"
    volumes:
      - larschat-data:/app/data
    restart: unless-stopped

volumes:
  larschat-data:
